# Python Dependencies - Security Patched
# Versions updated to remediate known CVEs

# Django - patched for SQL injection, XSS, DoS
Django>=4.2.21

# Requests - patched session handling
requests>=2.32.0

# PyYAML - patched arbitrary code execution
PyYAML>=5.4

# Pillow - patched buffer overflow, DoS
Pillow>=10.3.0

# urllib3 - patched ReDoS, header injection
urllib3>=2.2.2

# cryptography - patched multiple CVEs
cryptography>=44.0.0

# Jinja2 - patched XSS
Jinja2>=3.1.6

# Flask - patched session issues
Flask>=3.1.1

# Werkzeug - patched debug shell, DoS
Werkzeug>=3.1.3

# paramiko - patched Terrapin attack
paramiko>=3.4.0

# lxml - patched XSS, NULL pointer
lxml>=5.1.0

# httpx - patched open redirect
httpx>=0.23.0

# numpy - patched buffer overflow
numpy>=1.22

# aiohttp - patched multiple request smuggling
aiohttp>=3.11.18

# setuptools - patched ReDoS
setuptools>=78.1.1

# certifi - patched revoked root cert
certifi>=2024.7.4

# sqlparse - patched ReDoS
sqlparse>=0.5.0

# pycryptodome - patched side channel
pycryptodome>=3.19.1

# idna - patched resource consumption
idna>=3.7

# tornado - patched CRLF injection, open redirect
tornado>=6.5.5

# redis - patched connection handling
redis>=4.5.4

# python-jose - patched denial of service
python-jose>=3.4.0

# python-dotenv - patched path traversal
python-dotenv>=1.2.2

# Safe dependencies (latest versions, no known CVEs)
click==8.1.7
rich==13.7.1
pydantic==2.7.1
httptools==0.6.1
